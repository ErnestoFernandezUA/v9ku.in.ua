$(document).ready(function(){
  $('.slider').slick({
    arrows: false,
    dots: true,
    // slidesToShow: 4,
    // slidesToScroll: 1,
    // speed: 1000,
    infinite: false,
    // initialSlide: 0,
    centerMode: false,
    variableWidth: true,
    // draggable: false,
    touchThreshold: 10,
  });
});